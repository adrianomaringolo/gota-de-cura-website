# Dependencies whose install/postinstall scripts pnpm is allowed to run.
# firebase (Firestore) pulls in protobufjs; both need their build step.
allowBuilds:
  '@firebase/util': true
  protobufjs: true
